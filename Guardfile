# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'sass', :input => 'assets/css/scss', :output => 'assets/css', :style => 'compressed'

guard 'jekyll' do
  watch /.*/
  ignore /_site/
end
