sudo: false

language: node
node_js:
  - "6"

cache:
  directories:
    - node_modules

before_install:
  - rvm install 2.3.0
  - bundle install
  - npm install -g gulp gulp-cli
  - cat "production: true" > _config_local.yml
